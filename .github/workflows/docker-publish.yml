# ============================================================================
# 🐳 Docker Image Build & Publish
# ============================================================================
# Builds multi-platform Docker images and publishes to GitHub Container Registry.
# Generates attestations for supply chain security.
#
# Chain: CI → Release → Docker
# Triggers:
#   - On version tags (v*) created by Release workflow → Build & Push
#   - On PRs → Build only (validation)
# ============================================================================

name: 🐳 Docker Build & Publish

on:
  # Trigger on version tags created by semantic-release
  push:
    tags:
      - 'v*'
  # Build validation on PRs (no push)
  pull_request:
    branches:
      - master
      - main

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  # ============================================================================
  # 🏗️ Build & Push Docker Image
  # ============================================================================
  build-and-push:
    name: 🏗️ Build & Push
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      attestations: write
      id-token: write

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 🛠️ Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: 🛠️ Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: 🔐 Log in to GitHub Container Registry
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: 🏷️ Extract metadata (tags, labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=latest,enable=${{ github.ref_type == 'tag' }}
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}}
            type=sha,prefix=
            type=ref,event=pr

      - name: 📦 Build and push Docker image
        id: push
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          provenance: mode=min
          sbom: true

      - name: 🔏 Generate artifact attestation
        if: github.event_name != 'pull_request'
        uses: actions/attest-build-provenance@v2
        with:
          subject-name: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          subject-digest: ${{ steps.push.outputs.digest }}
          push-to-registry: true

      - name: 📊 Generate build summary
        run: |
          IS_PR="${{ github.event_name == 'pull_request' }}"
          IS_TAG="${{ github.ref_type == 'tag' }}"

          cat >> $GITHUB_STEP_SUMMARY << 'EOF'
          ## 🐳 Docker Image Build

          EOF

          if [ "$IS_PR" = "true" ]; then
            cat >> $GITHUB_STEP_SUMMARY << 'EOF'
          > [!NOTE]
          > ### 🔍 PR Build — Validation Only
          > Image built successfully but not pushed to registry.

          EOF
          elif [ "$IS_TAG" = "true" ]; then
            cat >> $GITHUB_STEP_SUMMARY << EOF
          > [!TIP]
          > ### ✅ Release Image Published
          > Version \`${{ github.ref_name }}\` has been pushed to GitHub Container Registry!

          EOF
          fi

          cat >> $GITHUB_STEP_SUMMARY << EOF
          ### 📋 Image Details

          | Property | Value |
          |:---------|:------|
          | 📦 Registry | \`${{ env.REGISTRY }}\` |
          | 🏷️ Image | \`${{ env.IMAGE_NAME }}\` |
          | 🏗️ Platforms | \`linux/amd64\`, \`linux/arm64\` |
          | 🔏 SBOM | Generated |
          | 📝 Provenance | Attached |

          ### 🏷️ Tags

          \`\`\`
          ${{ steps.meta.outputs.tags }}
          \`\`\`

          EOF

          if [ "$IS_PR" != "true" ]; then
            cat >> $GITHUB_STEP_SUMMARY << EOF
          ### 🚀 Quick Start

          \`\`\`bash
          docker pull ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.ref_name }}
          \`\`\`

          ### ⚙️ Workflow Chain

          \`\`\`
          🧪 CI Pipeline  ──→  🚀 Release  ──→  🐳 Docker Build
               ✅               ✅ v${{ github.ref_name }}      ✅
          \`\`\`

          EOF
          fi

          cat >> $GITHUB_STEP_SUMMARY << 'EOF'
          ---

          <sub>🤖 Built with Docker Buildx and GitHub Actions</sub>
          EOF
